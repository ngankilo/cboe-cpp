//
// Created by phamanhtan on 30/5/25.
//

#include "pitch/end_of_session.h"
#include <sstream>

namespace CboePitch {
    // EndOfSession EndOfSession::parse(const uint8_t *data, size_t length) {
    //     if (length < 1) throw std::runtime_error("EndOfSession message too short");
    //     return EndOfSession();
    // }

    // std::string EndOfSession::toString() const {
    //     return "EndOfSession {}";
    // }
} // namespace CboePitch
