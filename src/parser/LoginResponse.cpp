//
// Created by phamanhtan on 30/5/25.
//

#include "pitch/gap_login_response.h"
#include <sstream>

namespace CboePitch {
    // LoginResponse LoginResponse::parse(const uint8_t *data, size_t length) {
    //     if (length < 1) throw std::runtime_error("LoginResponse message too short");
    //     char status = static_cast<char>(data[0]);
    //     return LoginResponse(status);
    // }
    //
    // std::string LoginResponse::toString() const {
    //     std::stringstream ss;
    //     ss << "LoginResponse { Status: " << status << " }";
    //     return ss.str();
    // }
} // namespace CboePitch
